# Dependências para Privacidade Diferencial GPS + Blockchain

# Core dependencies
pandas>=2.0.0
numpy>=1.24.0

# Blockchain (Web3 + Ethereum)
web3>=6.0.0
eth-account>=0.10.0
py-solc-x>=2.0.0

# Geographic data and routing
osmnx>=1.6.0
networkx>=3.1

# Differential Privacy
diffprivlib>=0.6.0
# Alternativa: pydp>=1.1.4  (descomente se preferir PyDP do Google)

# Geospatial calculations
geopy>=2.4.0

# Machine Learning (necessário para osmnx spatial search)
scikit-learn>=1.3.0

# HTTP requests (para OSMnx)
requests>=2.31.0
